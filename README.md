# semantic-property-matching
Semantic property recommendation system that computes match scores between users and properties by converting structured and qualitative data into text and using sentence embeddings with cosine similarity.
